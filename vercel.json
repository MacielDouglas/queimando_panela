{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/graphql(.*)",
      "destination": "/api/index.js"
    },
    { "source": "/(.*)", "destination": "/" }
  ]
}
